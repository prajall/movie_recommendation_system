import React from "react";

const ShowMovies = ({ movies }) => {
  return (
    <div>
      {/* {movies.map((movie) => {
        <p>movie</p>;
      })} */}
    </div>
  );
};

export default ShowMovies;
